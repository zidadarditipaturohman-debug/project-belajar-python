# so i have study basic python, there is some new thing that i have to learn
# comment function, method, variable style, and something that biggest it is count also
# i have study too abt using \ \\ \\\.
# from idlelib import replace

print("my study result")
x = ("variable data") # this variable using () and "" for my computer using this data. and the tpe of data is string
y = 12345
# this data not using () and "" or ''. the name of this data is Intejer
u = True
# this type of data is Bollean. this type data just has two condition it is True or False and the first letter
# using capital
o = 3.53 # the name of this data is float. this numeric data with a number. the diperent with intejer is float have
# desimal

p = [1,2,3,4,5] #this a list of data , so we can put some dipperent type of data in there. also we can put many list of
#that contain with string, intejer, float

print(type(x))
print(type(y))
print(type(u))
print(type(o))
print(len(x)) # len is comment to count how many letter on string
print(x.count('data')) #count is module to count spesific word or letter on variable
price = ("$1.323,00")
print(price.replace('.','').replace('$','')) #so module replace can using twice on one command print